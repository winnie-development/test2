import { FaSearch } from "react-icons/fa";
import IconButton from ".";

const SearchButton = () => (
  <IconButton icon={FaSearch} label="Search" colorScheme="blue" />
);

export default SearchButton
